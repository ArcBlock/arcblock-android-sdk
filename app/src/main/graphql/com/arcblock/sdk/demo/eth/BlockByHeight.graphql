query blockByHeight($height:Int!){
  blockByHeight(height: $height) {
    transactions {
      data {
        traces {
          data {
            actionFunctionInput
          }
       }
      }
    }
  }
}