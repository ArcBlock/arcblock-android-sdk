query listBlocks($timeFilter:TimeFilter, $paging: PageInput){
  listBlocks(timeFilter: $timeFilter, paging: $paging){
    data{
      height
      hash
      numberTxs
    }
    page {
      cursor
      next
      total
    }
  }
}